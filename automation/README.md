# Automation

This area contains **operational scripts and automation helpers**.

## Contents

- `database-admin/` — automation moved from the previous DBA tree
- `ci-cd/` — CI/CD and pipeline helpers
- `deploy.sh`, `deploy.ps1` — top-level deployment wrappers

## Guidance

Put reusable scripts here when they are not tightly coupled to a single platform package.
